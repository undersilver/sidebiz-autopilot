# v2 安定化修正版

このZIPは、既存の `sidebiz-autopilot` リポジトリへ上書きするための修正版です。

## 主な修正

- 公開条件を `pass`、80点以上、highリスク0件、未解決出典なしに統一
- 本文に「要出典」「今後調査が必要」が残る場合は公開を強制停止
- リポジトリ所有者以外の `/revise` 実行を禁止
- 修正済み原稿だけを再校閲し、Issue本文とラベルを更新
- 公開処理で生成したファイルを、そのままPagesの成果物として使用
- 書き込みWorkflowを直列化し、自動コミット競合を抑制
- GitHub上で実在する `pikolon.png` へ画像参照を統一

## 検査済み

- Python全ファイルの構文
- Workflow全ファイルのYAML構文
- JSON設定ファイル
- 公開停止条件のロジック
- 1280×670サムネイル生成
- SVG旧参照の残存
- 秘密鍵・APIキーらしき文字列の混入

GitHub Modelsを含む外部API呼び出しは、GitHub上での実行確認が必要です。
